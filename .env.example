# URL de DB. Por defecto sqlite local para desarrollo:
DATABASE_URL=sqlite:///./app.db

# JWT config
SECRET_KEY=REEMPLAZA_POR_UNA_CADENA_LARGA_Y_ALEATORIA
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60   # expira en 60 minutos
REFRESH_TOKEN_EXPIRE_MINUTES=60*24*30  # (opcional) 30 días

# CORS: lista de orígenes permitidos separados por comas
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
